#include <stdio.h>
		
		
		int main(void)
	    int num1, num2, soma;
		{
			printf("Digite um numero\n");
			scanf ("%d", &num1);
			
			printf("Digite outro numero\n");
			scanf ("%d", &num2):
		}
		if (num %2 == 0);
			{
				
				soma = num + 2;
				printf ("O numero é par e a soma dele é: %d",soma);
				
			}
		else
		{	
			soma = num + 3;	
			printf ("O numero é impar e a soma dele é: %d", soma);	
			
		}
		
	return 0;
}
